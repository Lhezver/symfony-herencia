<form method="post" action="{{ path('app_hija_delete', {'id': hija.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hija.id) }}">
    <button class="btn">Delete</button>
</form>
